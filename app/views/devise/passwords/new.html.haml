

= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
  -# = f.error_notification
  - if resource.errors.any?
    %div.alert.alert-danger.errors= devise_error_messages!
  .form-inputs
    = f.text_field :email, required: true, placeholder: "Email", class: "login-input-field"
  .form-actions.form-actions-margin
    = f.button :submit, "パスワード再設定メールを送る", class: "btn black btn-base"
  -# .form-inputs
  -#   = f.input :email, required: true, autofocus: true
  -# .form-actions
  -#   = f.button :submit, "Send me reset password instructions"
-# = render "devise/shared/links"
